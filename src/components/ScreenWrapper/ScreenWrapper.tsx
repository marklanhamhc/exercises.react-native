import { BaseSize } from '@config';
import styled from 'styled-components/native';

export default styled.View`
  padding-top: ${BaseSize * 2}px;
  padding-right: ${BaseSize * 2}px;
  padding-bottom: ${BaseSize * 2}px;
  padding-left: ${BaseSize * 2}px;
`;
